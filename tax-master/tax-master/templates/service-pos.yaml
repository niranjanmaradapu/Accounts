apiVersion: v1
kind: Service
metadata:
  name: {{ include "tax-master.fullname" . }}-pos
  labels:
    {{- include "tax-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.postgres.type }}
  ports:
    - port: {{ .Values.service.postgres.port }}
  selector:
    {{- include "tax-master.selectorLabels" . | nindent 4 }}

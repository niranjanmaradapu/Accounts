apiVersion: v1
kind: Service
metadata:
  name: {{ include "hsn-details.fullname" . }}
  labels:
    {{- include "hsn-details.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "hsn-details.selectorLabels" . | nindent 4 }}

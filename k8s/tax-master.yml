apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: default
data:
  postgres_user: postgres
  postgres_password: 12345
  postgres_host: jdbc:postgresql://postgres:5432/postgres
  
---
#apiVersion: v1
#kind: PersistentVolumeClaim
#metadata:
#  name: postgres-pv-claim
#spec:
#  accessModes:
#    - ReadWriteOnce
#  resources:
#    requests:
#      storage: 1Gi
#---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-tax-master
spec:
  selector:
    matchLabels:
      app: postgres-tax-master
  template:
    metadata:
      labels:
        app: postgres-tax-master
    spec:
#      volumes:
#        - name: postgres-storage
#          persistentVolumeClaim:
#            claimName: postgres-pv-claim
      containers:
        - name: postgres-tax-master
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: postgres  # DB Created Initially 
          ports:
            - containerPort: 5432
              name: postgres-tax-master
#          volumeMounts:
#            - name: postgres-storage
#              mountPath: /var/lib/postgresql/data
          image: postgres  
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"    
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-tax-master
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres-tax-master
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tax-master-app
  namespace: default
spec:
  selector:
    matchLabels: 
      app: tax-master-app
  replicas: 1
  template:
    metadata:
      name: tax-master-app
      labels:
        app: tax-master-app
    spec:
      containers:
      - name: tax-master-app
        env:
          - name: SPRING_DATASOURCE_USERNAME
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_user
          - name: SPRING_DATASOURCE_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_password
          - name: SPRING_DATASOURCE_URL
#            value: jdbc:postgresql://postgres:5432/klm
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_host
        image: 881289302514.dkr.ecr.ap-south-1.amazonaws.com/tax-master # repository 
        resources:
          limits:
            memory: 512Mi
            cpu: "1"
          requests:
            memory: 256Mi
            cpu: "0.2"

---
apiVersion: v1
kind: Service
metadata:
  name: tax-master-app
spec:
  type: LoadBalancer
  ports:
    - port: 9191
  selector:
    app: tax-master-app        
